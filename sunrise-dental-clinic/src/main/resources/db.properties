# MySQL settings for XAMPP (default port 3306, user root, empty password)
db.url=jdbc:mysql://localhost:3306/sunrise_dental?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Colombo
db.username=root
db.password=
db.driver=com.mysql.cj.jdbc.Driver

# Must match the salt used when inserting staff passwords in database/sunrise_dental.sql
password.salt=SunriseDental#
